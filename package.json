{
  "name": "irflow-timeline",
  "version": "1.0.12",
  "description": "IRFlow Timeline — High-performance DFIR timeline viewer for Windows with AI Artifacts, Sigma detection, and native EVTX, MFT, USN Journal, and SQLite support.",
  "main": "electron/main.js",
  "author": "DFIR Community",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "dev": "concurrently \"vite\" \"TLE_DEV_SERVER=1 wait-on http://localhost:5173 && TLE_DEV_SERVER=1 electron .\"",
    "build:renderer": "vite build",
    "start": "npm run build:renderer && electron .",
    "bundle:hayabusa": "bash scripts/bundle-hayabusa.sh",
    "bundle:bmc-tools": "bash scripts/bundle-bmc-tools.sh",
    "bundle:tools": "npm run bundle:hayabusa && npm run bundle:bmc-tools",
    "fix:scripts": "node scripts/fix-line-endings.cjs",
    "dist": "npm run bundle:tools && npm run build:renderer && electron-builder --config electron-builder.config.cjs --mac",
    "dist:release": "npm run dist",
    "dist:smoke": "npm run bundle:tools && npm run build:renderer && SKIP_NOTARIZE=1 CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.cjs --mac",
    "dist:dmg": "npm run bundle:tools && npm run build:renderer && electron-builder --config electron-builder.config.cjs --mac dmg",
    "dist:universal": "npm run bundle:tools && npm run build:renderer && electron-builder --config electron-builder.config.cjs --mac --universal",
    "dist:win": "npm run build:renderer && electron-builder --config electron-builder.config.cjs --win",
    "dist:win:full": "npm run bundle:tools && npm run build:renderer && electron-builder --config electron-builder.config.cjs --win",
    "dist:win:nsis": "npm run build:renderer && electron-builder --config electron-builder.config.cjs --win nsis",
    "dist:win:portable": "npm run build:renderer && electron-builder --config electron-builder.config.cjs --win portable",
    "dist:win:ci": "npm run bundle:tools && npm run build:renderer && electron-builder --config electron-builder.config.cjs --win",
    "rebuild": "electron-rebuild -f -w better-sqlite3",
    "postinstall": "patch-package && electron-rebuild -f -w better-sqlite3",
    "smoke:electron": "electron scripts/smoke-electron-runtime.js",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "build": "vite build",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@electron/notarize": "^3.1.1",
    "@electron/rebuild": "^4.2.0",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.0",
    "electron": "^43.3.0",
    "electron-builder": "^26.15.3",
    "patch-package": "^8.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.0.7",
    "vitepress": "^1.6.4",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "@ts-evtx/core": "^1.1.1",
    "@ts-evtx/messages": "^1.1.1",
    "better-sqlite3": "^13.0.3",
    "csv-parser": "^3.0.0",
    "electron-updater": "^6.8.9",
    "exceljs": "^4.4.0",
    "js-yaml": "^4.3.1",
    "xlsx": "file:vendor/xlsx-0.20.3.tgz",
    "zustand": "^5.0.12"
  },
  "overrides": {
    "@ts-evtx/messages": {
      "better-sqlite3": "$better-sqlite3"
    }
  }
}
